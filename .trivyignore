# trivy config 예외 목록 — 각 항목은 사유·백로그 링크 필수 (무단 추가 금지)
#
# 현재 예외 없음. 모듈 이식 시 필요한 항목만 사유와 함께 추가한다.
# PoC repo(terraform-enterprise-poc)의 예외 2건은 그 repo의 설계 문서를 근거로 하므로
# 그대로 복사하지 않는다 — 해당 모듈을 이식·재검토한 뒤, 이 repo의 설계 문서를 근거로 다시 판단한다.
#
# 형식:
#   # <리소스/위치> — <사유>. 승인된 설계: docs/design/<문서>. 백로그: <링크>
#   AVD-AWS-XXXX
